boolean hasTransDuplicate=false;
for (int i=0;i<ctx._source.duplicates.length;i++) {
    if (ctx._source.duplicates[i].rules.length==0) {
         hasTransDuplicate = true
    }
}
ctx._source.hasTransDuplicate = hasTransDuplicate
