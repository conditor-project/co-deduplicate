ArrayList newDuplicate = new ArrayList();
if (ctx._source.duplicate != null && ctx._source.duplicate.length>0){
    int length = ctx._source.duplicate.length;
    for (int i=0;i<length;i++) {
        if (ctx._source.duplicate[i].idConditor!=params.idConditor) {
            newDuplicate.add(ctx._source.duplicate[i])
        }
    }
}
ctx._source.duplicate=newDuplicate
